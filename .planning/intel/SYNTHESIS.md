# Synthesis Summary

Entry point for downstream consumers (gsd-roadmapper). Generated by gsd-doc-synthesizer from a single-document ingest.

## Source documents
- 1 document ingested: `HANDOFF.md` (repo root) — classified **SPEC**, confidence **high**, precedence override **0** (highest), document-level `locked: false`.
- Document functions, in practice, as a combined PRD + SPEC + ADR/decision-log for a hackathon project ("Rider Payment Reconciler", AWS x WeMakeDevs "First Commit", Sept 17-20 2026). Content was extracted by actual nature (decision / requirement / constraint / running note) rather than forced entirely into one bucket, per ingest-run instruction.
- No `UNKNOWN` or low-confidence classifications. No other documents in the classification set — cycle detection trivially passes (single node; its cross_refs point outside the classified set).

## Decisions locked: 7
All extracted to `.planning/intel/decisions.md`, sourced from HANDOFF.md, and treated as locked per the document's own "don't relitigate without a new reason" / "don't reopen without new evidence" language:
1. Project idea selection (Own-delivery restaurant rider reconciliation — chosen; 6 alternatives explicitly dead/not-pursued)
2. Verdict rules are deterministic; the model never decides
3. Architecture and stack (Ship It track: ap-south-1, Python, SAM, Amplify/API Gateway/Lambda/Step Functions/DynamoDB/CloudWatch)
4. Scope boundaries (explicit out-of-scope list)
5. Repo integrity / competition compliance rules
6. Data handling and privacy rules
7. Framing rule — never use the word "fraud"

## Requirements extracted: 8
All extracted to `.planning/intel/requirements.md`, sourced from HANDOFF.md §3/§6/§9/§12:
REQ-shift-creation, REQ-photo-and-record-upload, REQ-reconciliation-run, REQ-per-rider-results, REQ-shift-level-results, REQ-resolve-flow, REQ-frontend, REQ-deliverables (README + video + writeup, each with detailed acceptance criteria).

## Constraints extracted: 11 (type breakdown)
All extracted to `.planning/intel/constraints.md`:
- schema (3): per-photo extraction output schema, DynamoDB single-table schema, S3 layout
- protocol (5): verdict rule table, exactly-once/ordering protocol, Bedrock extraction evaluation protocol, parser constraint (contingent on unanswered Q1), photo-timestamp constraint (contingent on unanswered Q2)
- api-contract (1): API endpoint contract (5 endpoints) + deferred-security note
- nfr (2): observability requirements, testing requirements

## Context topics: 9
All extracted to `.planning/intel/context.md`: Event and judging; Problem and user; Open questions for Brindhavan (Q1-Q5, UNANSWERED); Pitch; Novelty claim; Prior art (11 projects); Suggested repo layout; Timeline (+ risk list); Status checklist (UNANSWERED/incomplete); Owner background.

## Open items flagged (not resolved, not fabricated)
- **Q1-Q5** (HANDOFF.md §2): payment-record format, photo channel, shift scale, real mismatch example, filming/naming consent — all explicitly unanswered in the source. Captured verbatim in context.md; NOT converted into resolved requirements. Q1 and Q2 additionally drive contingent constraints in constraints.md (parser-behind-an-interface, screen-time-not-EXIF).
- **Status checklist** (HANDOFF.md §14): 10 items, all unchecked/blank in the source. Captured verbatim in context.md.

## Conflicts: 0 blockers, 0 competing-variants, 0 auto-resolved
Single-document ingest — no cross-document contradictions possible. Full detail (including the rationale for zero conflicts) in `.planning/INGEST-CONFLICTS.md`.

## Pointers
- Conflicts report: `.planning/INGEST-CONFLICTS.md`
- Decisions: `.planning/intel/decisions.md`
- Requirements: `.planning/intel/requirements.md`
- Constraints: `.planning/intel/constraints.md`
- Context: `.planning/intel/context.md`
