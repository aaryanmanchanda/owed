---
phase: 01-foundation-kickoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - template.yaml
  - samconfig.toml
  - src/api/hello/app.py
  - src/api/hello/requirements.txt
  - src/api/__init__.py
  - src/parse_record/__init__.py
  - src/extract/__init__.py
  - src/reconcile/__init__.py
  - src/common/__init__.py
  - statemachine/.gitkeep
  - web/.gitkeep
  - tests/__init__.py
  - tests/unit/__init__.py
  - tests/concurrency/__init__.py
  - tests/fixtures/README.md
  - requirements-dev.txt
  - README.md
  - docs/DECISIONS.md
  - docs/AI_TOOLS.md
  - docs/extraction-eval.md
  - testdata/README.md
autonomous: false
requirements: [SC-1, SC-5, D-01, D-02, D-03]

user_setup:
  - service: aws
    why: "New free-tier AWS account (D-03) replaces the suspended original (D-02). Every deploy in this phase and every later phase runs against it. Claude must never see or handle the access keys."
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "AWS Console -> IAM -> Users -> (your user) -> Security credentials -> Create access key. Entered via `aws configure`, never pasted into a file Claude reads."
      - name: AWS_SECRET_ACCESS_KEY
        source: "Shown once at access-key creation time. Entered via `aws configure`."
      - name: AWS_DEFAULT_REGION
        source: "Set to ap-south-1 during `aws configure` (HANDOFF §9 locks the region)."
    dashboard_config:
      - task: "Confirm the new account is out of any signup hold and can create CloudFormation stacks"
        location: "AWS Console -> CloudFormation (ap-south-1) -> the Stacks list loads without a billing/verification banner"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Curling the deployed ap-south-1 HTTP API URL returns HTTP 200 with a JSON body containing a message field (ROADMAP SC-1)."
    - "The deployed hello handler reports ap-south-1 as its own execution region, proving the stack landed in the locked region (HANDOFF §9)."
    - "The repo tree matches the HANDOFF §11 layout skeleton and every commit postdates the 2026-09-17 kickoff (ROADMAP SC-5, HANDOFF §1)."
    - "docs/DECISIONS.md exists with dated first entries covering the account swap (D-02/D-03), the Day-1 smoke-test decision (D-05), and the test-data handling rule (PROJECT.md Working Rule 6)."
    - "Real payment photos and the raw transaction export cannot be committed: testdata/ contents are ignored by git before any such file lands."
  artifacts:
    - template.yaml
    - samconfig.toml
    - src/api/hello/app.py
    - .gitignore
    - docs/DECISIONS.md
    - docs/AI_TOOLS.md
    - README.md
    - tests/unit/__init__.py
    - requirements-dev.txt
    - testdata/README.md
  key_links:
    - "HttpApi route GET /hello -> HelloWorldFunction: the $default stage means the invoke URL has NO stage prefix; a /Prod/hello URL returns 404."
    - "template.yaml Runtime python3.13 -> a local python3.13 binary on PATH: sam build's PythonPipBuilder rejects the build without it."
    - ".gitignore testdata/* rule -> must exist and be committed BEFORE the first photo or bank export is saved locally."
    - "CloudFormation output ApiUrl -> the curl verification command; without the output the endpoint cannot be discovered non-interactively."
---

<objective>
Prove the locked AWS plumbing deploys and answers, and put the HANDOFF §11 repo skeleton on disk with a real decision log started.

This is the phase's tracer: one thin path from `template.yaml` through CloudFormation, API Gateway (HTTP API) and Lambda to an HTTP 200 in ap-south-1. Everything the rest of Phase 1 and Phases 2-5 build sits on top of that path, so it gets proven first and committed before anything else is layered on.

Per D-01 this is day 2 of a 3.5-day event and Phase 1 is starting a day behind — move with urgency, do not gold-plate. Per D-02/D-03 the original AWS account was suspended and has been replaced by a new free-tier account under a different email; Task 1 is the human gate that makes that account usable from this machine.

Purpose: satisfies ROADMAP Phase 1 success criteria 1 and 5, and unblocks plan 01-02 (Bedrock) and 01-03 (test data / outreach), which both run in wave 2 per D-04.
Output: a deployed `owed-foundation` stack in ap-south-1 returning 200, plus a committed repo skeleton with `docs/DECISIONS.md` started.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-kickoff/01-CONTEXT.md
@.planning/phases/01-foundation-kickoff/01-RESEARCH.md
@HANDOFF.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: AWS toolchain, new-account credentials, and seed photos</name>
  <files>~/.aws/credentials, ~/.aws/config, testdata/photos/ (all outside git; no repo file is written by this task)</files>
  <read_first>
    - `.planning/phases/01-foundation-kickoff/01-RESEARCH.md` section "Environment Availability" — `sam`, `aws` and AWS credentials are all confirmed ABSENT on this machine; local Python is 3.9.6 and Docker's daemon is not running.
    - `.planning/phases/01-foundation-kickoff/01-CONTEXT.md` decisions D-02 and D-03 — why the account changed.
    - `HANDOFF.md` §6 "Data rules" — test photos are Aaryan's own small transfers photographed off a phone screen; no customer screens, ever.
  </read_first>
  <action>Make the new AWS free-tier account (D-03) usable from this machine and put one real seed photo on disk. Claude has nothing to automate first: `sam`, `aws` and AWS credentials are all absent per RESEARCH "Environment Availability", `aws configure` is an interactive credential prompt Claude must never drive (threat T-01-01), and photographing a payment screen is physical. Present the instructions below and wait.</action>
  <instructions>
    I cannot start this one — there is nothing automatable ahead of it. `sam`, `aws` and your AWS credentials are all missing on this machine, I must never handle your access keys, and the seed photo is physical.

    Please run these three, in your own terminal, from the repo root:

    1. `brew install awscli aws-sam-cli python@3.13`
       The first two are mandatory: RESEARCH "Environment Availability" records both as absent with no fallback, and SAM is locked by HANDOFF §9. `python@3.13` matters because `template.yaml` declares `Runtime: python3.13` and `sam build`'s PythonPipBuilder searches PATH for a matching `python3.13` binary and aborts if it is missing. The documented alternative is `sam build --use-container` with Docker Desktop running, which RESEARCH marks unnecessary here since every Lambda in this project is pure Python.

    2. `aws configure`
       Use the access key for the NEW free-tier account you created on 2026-09-18 (D-03), NOT the suspended original (D-02). Enter `ap-south-1` as the default region name — HANDOFF §9 locks it. Default output format `json`.

    3. Take 2 or 3 quick photos of your own UPI payment-success screens, photographed off a phone screen, and save them under `testdata/photos/` in the repo root (create the directory if needed).
       These are the seed photos plan 01-02's Bedrock smoke test invokes against; the full 20-30 photo set is plan 01-03's job. Do not photograph any customer's payment screen — HANDOFF §6 and PROJECT.md Constraints forbid it.

    Why each one blocks: without 1 nothing can build or deploy, without 2 nothing can authenticate, without 3 the wave-2 Bedrock smoke test has no image to read from S3.

    One thing to be aware of, no action needed: `testdata/` is not ignored by git yet — Task 2 writes and commits the `.gitignore` that ignores it. Please do not `git add` anything until Task 2 has run.
  </instructions>
  <verification>`aws sts get-caller-identity` returns a 12-digit `Account`, `aws configure get region` prints `ap-south-1`, `sam --version` and `command -v python3.13` both succeed, and at least one image file exists under `testdata/photos/`.</verification>
  <resume-signal>Type "done" once all three are finished, or tell me which one failed and what it said.</resume-signal>
  <acceptance_criteria>
    - `aws sts get-caller-identity` returns a JSON object whose `Account` field is a non-empty 12-digit account ID.
    - `aws configure get region` prints exactly `ap-south-1`.
    - `sam --version` prints a version string and exits 0.
    - `command -v python3.13` prints a path.
    - At least one file exists under `testdata/photos/` with a `.jpg`, `.jpeg` or `.png` extension.
  </acceptance_criteria>
  <verify>
    <automated>aws sts get-caller-identity --output json > /tmp/owed-whoami.json && python3 -c "import json,sys;d=json.load(open('/tmp/owed-whoami.json'));sys.exit('NO_ACCOUNT') if not d.get('Account') else print('ACCOUNT_OK',d['Account'])" && test "$(aws configure get region)" = "ap-south-1" && sam --version && command -v python3.13 && ls testdata/photos/ | grep -Eiq '\.(jpe?g|png)$' && echo BOOTSTRAP_OK</automated>
    <fails_when>non-zero exit; `BOOTSTRAP_OK` absent from the output; `NO_ACCOUNT` printed; `Unable to locate credentials`, `ExpiredToken` or `UnrecognizedClientException` on stderr; `aws configure get region` printing anything other than `ap-south-1`; `command -v python3.13` printing nothing (sam build will then reject the python3.13 runtime); or the `ls testdata/photos/` grep finding no image file.</fails_when>
  </verify>
  <done>The new AWS account authenticates from this machine against ap-south-1, `sam`/`aws`/`python3.13` are all on PATH, and at least one seed photo of Aaryan's own UPI payment exists locally.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end "deployed endpoint answers 200" — one path only</name>
  <files>.gitignore, template.yaml, samconfig.toml, src/api/hello/app.py, src/api/hello/requirements.txt</files>
  <precondition>The AWS CLI resolves credentials for the new free-tier account and `aws configure get region` reports `ap-south-1` (established by Task 1; D-03 and D-09 make every AWS call in this phase depend on it).</precondition>
  <read_first>
    - `HANDOFF.md` §9 "Architecture (Ship It)" — the locked chain is API Gateway (HTTP API) -> Lambda; region ap-south-1; runtime Python; IaC AWS SAM. This task builds exactly the first hop of that chain and nothing else.
    - `.planning/phases/01-foundation-kickoff/01-RESEARCH.md` section "SAM hello-world initialization" — the official tutorial's flow and the post-deploy `curl` check.
    - `.planning/phases/01-foundation-kickoff/01-RESEARCH.md` "Assumptions Log" A1 and A6 — Python 3.13 runtime choice and the admin-credential assumption for the first `sam deploy`.
  </read_first>
  <action>
    Wire ONE request path through every layer this phase touches: local IaC -> CloudFormation -> HTTP API -> Lambda -> HTTP 200. No second route, no authorizer, no extra function. This is production-quality scaffolding that survives into Phase 3, not a prototype.

    Do NOT run `sam init` — it is interactive and would drop an unrelated starter tree into the repo root. Hand-write the four files below.

    `.gitignore` (write this FIRST, before any other file in this task, and make it the first thing staged): ignore `.aws-sam/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.venv/`, `venv/`, `.env`, `.env.*`, `*.pem`, `.DS_Store`, `node_modules/`, `packaged.yaml`, and the two-line pair `testdata/*` followed by `!testdata/README.md` so the directory's documentation stays tracked while every photo and bank export inside it stays out of git (threat T-01-02). `samconfig.toml` is deliberately NOT ignored — it carries no secrets and pins the deploy parameters.

    `template.yaml` at the repo root: `AWSTemplateFormatVersion: '2010-09-09'`, `Transform: AWS::Serverless-2016-10-31`, `Description` naming the project `owed`. A `Globals` block setting `Function.Timeout: 10`, `Function.MemorySize: 256`, `Function.Runtime: python3.13`, `Function.Architectures: [arm64]` (arm64 Graviton is the cheaper option with no native dependencies in this project — a cost lever to record for README section 7). Resources: `OwedHttpApi` of type `AWS::Serverless::HttpApi` with no `StageName` property so SAM creates the `$default` stage and the invoke URL carries no stage prefix; and `HelloWorldFunction` of type `AWS::Serverless::Function` with `FunctionName: owed-hello`, `CodeUri: src/api/hello/`, `Handler: app.lambda_handler`, and a single event named `HelloGet` of `Type: HttpApi` with `ApiId: !Ref OwedHttpApi`, `Path: /hello`, `Method: GET`. Outputs: `ApiUrl`, value `!Sub "https://${OwedHttpApi}.execute-api.${AWS::Region}.amazonaws.com/hello"`, and `HelloFunctionName`, value `!Ref HelloWorldFunction`. Do not attach any explicit IAM policy to `HelloWorldFunction` — SAM's default execution role is CloudWatch-Logs-only, which is the least-privilege answer for a handler with no AWS calls (threat T-01-03).

    `src/api/hello/app.py`: a `lambda_handler(event, context)` that returns a dict with `statusCode` 200, a `Content-Type: application/json` header, and a `body` produced by `json.dumps` of a mapping with key `message` set to a short literal greeting and key `region` read from the `AWS_REGION` environment variable. Returning the region is what makes the 200 prove the stack landed in ap-south-1 rather than a default region. Import only `json` and `os`.

    `src/api/hello/requirements.txt`: a single comment line stating that boto3 ships in the Lambda managed runtime so this handler pins nothing. An empty dependency set keeps the build fast and avoids shipping a redundant SDK copy.

    `samconfig.toml` at the repo root: SAM CLI version `0.1` config with a `[default.deploy.parameters]` table pinning `stack_name = "owed-foundation"`, `region = "ap-south-1"`, `capabilities = "CAPABILITY_IAM"`, `resolve_s3 = true`, `confirm_changeset = false`, `fail_on_empty_changeset = false`. Pinning these means every later deploy in this phase and in Phase 3 is reproducible and non-interactive.

    Then run `sam build` followed by `sam deploy --no-confirm-changeset --no-fail-on-empty-changeset`. Never pass `--guided`: it is interactive and cannot complete in an autonomous executor.

    Commit as one atomic commit with an honest message (HANDOFF §15 rule 2). Never pass `--date`, never amend an earlier commit, never squash — a history that does not match the event dates disqualifies the entry (HANDOFF §1, threat T-01-04).
  </action>
  <acceptance_criteria>
    - `sam build` exits 0 and creates `.aws-sam/build/HelloWorldFunction/app.py`.
    - `sam deploy` exits 0 and the CloudFormation stack `owed-foundation` reaches `CREATE_COMPLETE` or `UPDATE_COMPLETE` in ap-south-1.
    - The stack exposes an output named `ApiUrl` whose value ends in `/hello` and contains `execute-api.ap-south-1.amazonaws.com`.
    - `curl` against that URL returns HTTP status `200`.
    - The returned JSON body contains a `message` key and a `region` key whose value is `ap-south-1`.
    - `git check-ignore testdata/photos` exits 0, proving the seed photos from Task 1 are outside git.
  </acceptance_criteria>
  <verify>
    <automated>sam build && sam deploy --no-confirm-changeset --no-fail-on-empty-changeset && URL=$(aws cloudformation describe-stacks --stack-name owed-foundation --region ap-south-1 --query "Stacks[0].Outputs[?OutputKey=='ApiUrl'].OutputValue" --output text) && echo "URL=$URL" && test -n "$URL" && curl -sS -o /tmp/owed-hello.json -w '%{http_code}\n' "$URL" | grep -qx 200 && python3 -c "import json,sys;d=json.load(open('/tmp/owed-hello.json'));sys.exit('BAD_BODY '+str(d)) if d.get('region')!='ap-south-1' or 'message' not in d else print('TRACER_OK')"</automated>
    <fails_when>non-zero exit; `TRACER_OK` absent from the output; the printed status line is anything other than `200` (a 404 means the `$default` stage prefix or the `/hello` route does not match, a 403 means the route exists but the integration is wrong); `URL=` printed with nothing after it (the `ApiUrl` output is missing from the stack); `BAD_BODY` printed (the handler answered from the wrong region or omitted `message`); or `sam build` printing `Binary validation failed for python` (the local `python3.13` binary from Task 1 is not on PATH).</fails_when>
    <automated>git check-ignore -q testdata/photos && echo PHOTOS_IGNORED</automated>
    <fails_when>non-zero exit or `PHOTOS_IGNORED` absent — meaning `.gitignore` does not cover `testdata/`, and the next `git add` would commit real payment-screen photos (threat T-01-02).</fails_when>
  </verify>
  <done>A real ap-south-1 HTTP API endpoint answers HTTP 200 with a JSON body reporting `ap-south-1`, the stack is deployed from committed IaC, and `testdata/` is provably outside git. ROADMAP Phase 1 success criterion 1 is met.</done>
  <reversibility rating="reversible">Stack name, runtime version, memory and architecture are all one-line `template.yaml` edits plus a redeploy; the whole stack is deletable with `sam delete`.</reversibility>
</task>

<task type="auto">
  <name>Task 3: HANDOFF §11 skeleton and the running decision log</name>
  <files>src/api/__init__.py, src/parse_record/__init__.py, src/extract/__init__.py, src/reconcile/__init__.py, src/common/__init__.py, statemachine/.gitkeep, web/.gitkeep, tests/__init__.py, tests/unit/__init__.py, tests/concurrency/__init__.py, tests/fixtures/README.md, testdata/README.md, requirements-dev.txt, README.md, docs/DECISIONS.md, docs/AI_TOOLS.md, docs/extraction-eval.md</files>
  <read_first>
    - `HANDOFF.md` §11 "Suggested repo layout" — the exact tree to materialize, including the note that `reconcile/` stays pure with the DynamoDB layer separate.
    - `HANDOFF.md` §12 "Deliverables" — the ordered 11-section README structure the stub must pre-declare.
    - `.planning/PROJECT.md` "Working Rules" rule 6 — a `docs/DECISIONS.md` entry accompanies every decision made or changed.
    - `.planning/phases/01-foundation-kickoff/01-VALIDATION.md` "Wave 0 Requirements" — the `tests/` package scaffold and `requirements-dev.txt` pin this task owes Phase 3.
  </read_first>
  <action>
    Materialize the rest of the HANDOFF §11 tree around the tracer, so Phase 3 never has to restructure.

    Create package markers, each containing a single one-line docstring naming which phase fills it: `src/api/__init__.py` (API Lambda handlers, Phase 3), `src/parse_record/__init__.py` (parser interface plus one implementation, Phase 3, blocked on Q1), `src/extract/__init__.py` (production Bedrock call, prompt and validation, Phase 3 — explicitly NOT the throwaway smoke-test Lambda plan 01-02 adds at `src/_smoketest/`, per D-06), `src/reconcile/__init__.py` (pure verdict engine with the DynamoDB claim layer kept separate, Phase 3), `src/common/__init__.py` (models, logging, config).

    Create empty placeholders `statemachine/.gitkeep` and `web/.gitkeep` so the directories track before Phase 3 and Phase 4 fill them.

    Create `tests/__init__.py`, `tests/unit/__init__.py`, `tests/concurrency/__init__.py`, and `tests/fixtures/README.md`. The fixtures README must state the rule that governs that directory: it holds only synthetic credits and hand-written extraction JSON (HANDOFF §11), never a raw bank export and never a payment-screen image; real photos and the raw transaction export live in the git-ignored `testdata/` directory instead.

    Create `testdata/README.md` (tracked via the `.gitignore` negation from Task 2) describing what belongs there: Aaryan's own UPI payment photos, his raw transaction export, and scratch invoke payloads — all git-ignored because they contain his real personal banking data.

    Create `requirements-dev.txt` pinning `pytest>=8,<9`. Do not add `moto` or `hypothesis` yet — `01-VALIDATION.md` Wave 0 defers those pins to Phase 3, when there is code to exercise them.

    Create `docs/AI_TOOLS.md` with a dated running list of AI coding tools used, seeded with the tool actually in use for this session. HANDOFF §1 makes this list a submission requirement, and §15 rule 3 requires crediting anything not written during the event.

    Create `docs/extraction-eval.md` as a headings-only stub for the §8 evaluation with an explicit line saying no numbers have been measured yet. Write no accuracy, latency or cost figures — PROJECT.md Working Rule 5 forbids any number that was not actually measured, and Phase 2 is what measures them.

    Create `README.md` as a design-doc stub carrying the eleven HANDOFF §12 section headings in their required order, each with a one-line note on what fills it and in which phase. Phase 5 writes the prose.

    Create `docs/DECISIONS.md` as a running log, newest first, each entry carrying the date `2026-09-18`, the decision, the rationale, and the source reference. Seed it with exactly these entries: (a) the original AWS account was suspended for an unpaid post-trial balance and has been replaced by a new free-tier account under a different email, so all AWS work runs there — cite D-02 and D-03, and record that Builder Center re-verification and $100-credit eligibility under the new email remain OPEN, not resolved; (b) a Day-1 Bedrock smoke test was added to Phase 1 to retire the S3-bytes-into-Bedrock IAM risk early — cite D-05 and HANDOFF §13 risk 2; (c) the smoke test calls Bedrock from inside a Lambda that reads the image bytes from S3 itself, and never uses Step Functions' direct Bedrock integration for images — cite D-06 and HANDOFF §9; (d) the smoke test's pass bar is a schema-valid response only, with accuracy deferred to Phase 2's evaluation — cite D-07; (e) the smoke-test model is whichever candidate grants inference access first in ap-south-1, with the real comparison deferred to Phase 2 — cite D-08; (f) real payment photos and the raw transaction export are kept out of git under `testdata/`, while `tests/fixtures/` holds only value-scrubbed shape-faithful data — cite HANDOFF §6 and §11; (g) Lambda functions default to arm64 at 256 MB as a deliberate cost lever for README section 7; (h) the first deploys use the new account's initial administrator credentials on the dev machine, recorded as a deliberate Day-1 shortcut rather than a production pattern — cite the RESEARCH Security Domain note; (i) the repo is kept public with honest post-kickoff history and will be pushed to a public GitHub remote before Saturday's rough submission — cite HANDOFF §1 and the repo-visibility discretion left open in 01-CONTEXT.md.

    Commit as a single atomic commit separate from Task 2's.
  </action>
  <acceptance_criteria>
    - Every path in the HANDOFF §11 tree exists: `README.md`, `template.yaml`, `statemachine/`, `src/api/`, `src/parse_record/`, `src/extract/`, `src/reconcile/`, `src/common/`, `web/`, `tests/unit/`, `tests/concurrency/`, `tests/fixtures/`, `docs/DECISIONS.md`, `docs/AI_TOOLS.md`, `docs/extraction-eval.md`.
    - `docs/DECISIONS.md` contains at least nine dated entries and cites each of D-02, D-03, D-05, D-06, D-07 and D-08 by identifier.
    - `README.md` contains all eleven HANDOFF §12 section headings.
    - `requirements-dev.txt` pins pytest and does not yet pin moto or hypothesis.
    - `git ls-files` lists `testdata/README.md` and lists no file under `testdata/photos/`.
    - `git log` shows every commit in the repository authored on or after 2026-09-17 (kickoff).
  </acceptance_criteria>
  <verify>
    <automated>for p in README.md template.yaml samconfig.toml statemachine src/api/__init__.py src/parse_record/__init__.py src/extract/__init__.py src/reconcile/__init__.py src/common/__init__.py web tests/__init__.py tests/unit/__init__.py tests/concurrency/__init__.py tests/fixtures/README.md requirements-dev.txt docs/DECISIONS.md docs/AI_TOOLS.md docs/extraction-eval.md testdata/README.md; do test -e "$p" || { echo "MISSING_PATH $p"; exit 1; }; done && echo SKELETON_OK</automated>
    <fails_when>non-zero exit, or a line beginning `MISSING_PATH` naming the first §11 path that was not created; `SKELETON_OK` absent means the loop aborted early.</fails_when>
    <automated>for d in D-02 D-03 D-05 D-06 D-07 D-08; do grep -q "$d" docs/DECISIONS.md || { echo "UNCITED_DECISION $d"; exit 1; }; done && test "$(grep -c '^### ' docs/DECISIONS.md)" -ge 9 && echo DECISIONS_OK</automated>
    <fails_when>non-zero exit; a line beginning `UNCITED_DECISION` naming a CONTEXT decision that was not logged; or `DECISIONS_OK` absent because fewer than nine `###` entry headings exist in the log.</fails_when>
    <automated>git ls-files testdata > /tmp/owed-td-tracked.txt; test $? -eq 0 || { echo GIT_LS_FAILED; exit 1; }; git log --format='%ad' --date=format:'%Y-%m-%d' > /tmp/owed-commit-dates.txt; test $? -eq 0 || { echo GIT_LOG_FAILED; exit 1; }; grep -qx 'testdata/README.md' /tmp/owed-td-tracked.txt && test "$(wc -l < /tmp/owed-td-tracked.txt | tr -d ' ')" = "1" && test -z "$(awk '$1 < "2026-09-17"' /tmp/owed-commit-dates.txt)" && echo INTEGRITY_OK</automated>
    <fails_when>non-zero exit; `INTEGRITY_OK` absent; `GIT_LS_FAILED` or `GIT_LOG_FAILED` printed (the git command itself errored rather than returning an empty result); a tracked-path count other than 1 under `testdata`, meaning a real photo or bank export was committed (threat T-01-02); or the `awk` filter printing a date, meaning a commit predates the 2026-09-17 kickoff and would disqualify the entry (HANDOFF §1).</fails_when>
  </verify>
  <done>The repo tree matches HANDOFF §11, `docs/DECISIONS.md` carries dated first entries citing D-02, D-03, D-05, D-06, D-07 and D-08, all history postdates kickoff, and nothing under `testdata/` except its README is tracked. ROADMAP Phase 1 success criterion 5 is met.</done>
  <reversibility rating="reversible">Directory placement and stub content are plain file moves; nothing downstream is committed to them yet.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01-01)

| Artifact | Kind | Notes |
|---|---|---|
| `template.yaml` | SAM template | Root IaC. Adds `OwedHttpApi` (`AWS::Serverless::HttpApi`, `$default` stage) and `HelloWorldFunction` (`owed-hello`). Plan 01-02 extends the same file. |
| `src/api/hello/app.py` | Lambda handler | `lambda_handler` returning 200 with `message` and `region`. |
| `src/api/hello/requirements.txt` | Dependency manifest | Intentionally dependency-free. |
| `samconfig.toml` | SAM deploy config | Pins stack `owed-foundation`, region `ap-south-1`, non-interactive deploy flags. |
| `.gitignore` | Repo config | Ignores `.aws-sam/`, `testdata/*` with a `!testdata/README.md` negation, caches, env and key files. |
| CloudFormation stack `owed-foundation` | Deployed AWS resource | Outputs `ApiUrl`, `HelloFunctionName`. |
| IAM role (SAM-generated) for `owed-hello` | Deployed AWS resource | CloudWatch-Logs-only default; no explicit policy attached. |
| `src/{api,parse_record,extract,reconcile,common}/__init__.py` | Package markers | HANDOFF §11 skeleton; each names its owning phase. |
| `statemachine/.gitkeep`, `web/.gitkeep` | Placeholders | Phase 3 and Phase 4 fill these. |
| `tests/__init__.py`, `tests/unit/__init__.py`, `tests/concurrency/__init__.py` | Test packages | 01-VALIDATION.md Wave 0 scaffold. |
| `tests/fixtures/README.md` | Doc | States the synthetic-only rule for that directory. |
| `testdata/README.md` | Doc | Only tracked file under the ignored `testdata/` tree. |
| `requirements-dev.txt` | Dependency manifest | `pytest>=8,<9`. |
| `README.md` | Deliverable stub | Eleven HANDOFF §12 headings in order. |
| `docs/DECISIONS.md` | Running decision log | Nine dated seed entries citing D-02, D-03, D-05, D-06, D-07, D-08. |
| `docs/AI_TOOLS.md` | Deliverable | Dated running list of AI coding tools used. |
| `docs/extraction-eval.md` | Deliverable stub | Headings only; Phase 2 measures the numbers. |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> API Gateway HTTP API | Unauthenticated GET /hello reaches a Lambda; no accounts exist by design (HANDOFF §9 defers Cognito). |
| developer machine -> AWS control plane | Long-lived access keys for the new account authorize CloudFormation, IAM role creation and S3 writes. |
| working tree -> git history -> public GitHub | Anything staged becomes permanent public record; the repo is a submission deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | AWS access keys on the dev machine / in the repo | high | mitigate | Credentials are entered only via `aws configure` in Task 1's human checkpoint; Claude never reads or writes them. `.gitignore` covers `.env`, `.env.*` and `*.pem`. `samconfig.toml` is committed but carries only stack name, region and capability flags — no key material. |
| T-01-02 | Information Disclosure | Aaryan's real UPI payment photos under `testdata/` | high | mitigate | `.gitignore` with `testdata/*` plus `!testdata/README.md` is written and staged FIRST in Task 2, before any photo can be added; Task 2 and Task 3 each verify with `git check-ignore` and a `git ls-files testdata` count of exactly one. |
| T-01-03 | Elevation of Privilege | `owed-hello` Lambda execution role | low | accept | The handler makes no AWS API calls, so SAM's default `AWSLambdaBasicExecutionRole` (CloudWatch Logs only) is already minimal. No explicit policy is attached and none is needed at ASVS L1. |
| T-01-04 | Repudiation | Git history vs. event dates | medium | mitigate | One atomic commit per task with honest messages; `--date`, `--amend` on pushed commits and squashing are forbidden (HANDOFF §15 rule 2). Task 3 verifies no commit predates 2026-09-17. |
| T-01-05 | Denial of Service | Public unauthenticated GET /hello | low | accept | A hello endpoint with no backing data and a 10-second timeout on a free-tier account; API Gateway's default account-level throttle applies. The shared-secret header and throttle config HANDOFF §9 requires belong to the real API surface in Phase 3, not to this tracer. |
| T-01-SC | Tampering | brew / pip installs (`awscli`, `aws-sam-cli`, `python@3.13`, `pytest`) | high | mitigate | `01-RESEARCH.md` "Package Legitimacy Audit" covers `boto3`, `aws-sam-cli`, `pytest`, `moto`, `hypothesis`; all five `[SUS]` verdicts are documented heuristic false positives explicitly overridden to OK against official org-owned repositories (`aws`, `boto`, `pytest-dev`, `getmoto`). No `[SLOP]` package is present and no unresolved `[ASSUMED]`/`[SUS]` package remains, so no blocking legitimacy checkpoint is required. `awscli` and `python@3.13` are Homebrew core formulae installed by the user in Task 1. |
</threat_model>

<source_coverage_audit>
## Multi-source Coverage Audit (phase 01, all three plans)

**GOAL — ROADMAP Phase 1 success criteria**

| Item | Status | Where |
|---|---|---|
| SC-1 SAM hello-world returns HTTP 200 from ap-south-1 | COVERED | 01-01 Task 2 |
| SC-2 Bedrock access in ap-south-1 confirmed / inference profile identified, trade-off noted | COVERED | 01-02 Tasks 1 and 3 |
| SC-3 20-30 test photos + fixture from Aaryan's own transaction export | COVERED | 01-03 Tasks 1 and 3 |
| SC-4 Q1-Q5 asked, answers or non-answers recorded in `.planning/intel/context.md` | COVERED | 01-03 Tasks 2 and 3 |
| SC-5 Repo skeleton, post-kickoff first commit, `docs/DECISIONS.md` started | COVERED | 01-01 Tasks 2 and 3 |

**REQ — phase requirement IDs:** none. REQUIREMENTS.md Traceability marks Phase 1 an infrastructure-enabling gate. No REQ-ID is unplanned.

**RESEARCH — 01-RESEARCH.md features and constraints**

| Item | Status | Where |
|---|---|---|
| Environment Availability: `sam`, `aws`, credentials absent; local Python 3.9.6 vs. runtime 3.13 | COVERED | 01-01 Task 1 (adds `python@3.13`, documents the `--use-container` fallback) |
| Pattern 1: Lambda reads S3 bytes then calls Bedrock Converse | COVERED | 01-02 Task 2 |
| IAM: `s3:GetObject` scoped, `bedrock:InvokeModel`, destination-region ARNs | COVERED | 01-02 Task 2 |
| Open question 1: exact ap-south-1 inference-profile ID string | COVERED | 01-02 Task 1 (live lookup, with a stated fallback default) |
| Open question 2: Anthropic one-time use-case form under the new account | COVERED | 01-02 Task 1 |
| Open question 3: exact Sunday submission deadline time | COVERED | 01-03 Task 2 |
| Pitfall 1/2: bare model ID vs. inference profile; access vs. regional invocation path | COVERED | 01-02 Tasks 1 and 3 |
| Pitfall 3: record the region that actually processed the request | COVERED | 01-02 Task 3 |
| Pitfall 4: Builder Center re-verification and $100-credit eligibility under the new email | COVERED | 01-03 Task 2 (human confirmation only — explicitly not solved programmatically) |
| Security Domain: SSE, block public access, least-privilege role, admin-credential shortcut logged | COVERED | 01-01 Task 3 entry (h), 01-02 Task 2 |
| Wave 0 gaps: `tests/` package scaffold, `requirements-dev.txt` pytest pin | COVERED | 01-01 Task 3 |
| Package Legitimacy Audit (five `[SUS]` overrides, no `[SLOP]`) | COVERED | `T-01-SC` in all three plans' threat registers |

**CONTEXT — 01-CONTEXT.md locked decisions**

| Decision | Status | Where |
|---|---|---|
| D-01 day-2 urgency, no compressed deadline | COVERED | 01-01 objective; wave structure keeps 01-02 and 01-03 parallel |
| D-02 original account suspended | COVERED | 01-01 Task 1, `docs/DECISIONS.md` entry (a) |
| D-03 new free-tier account under a different email | COVERED | 01-01 Task 1, `user_setup` |
| D-04 AWS and non-AWS tracks proceed in parallel | COVERED | 01-02 and 01-03 both sit in wave 2 |
| D-05 Day-1 Bedrock smoke test added | COVERED | 01-02 objective and Task 3 |
| D-06 Lambda reads S3 bytes and calls Bedrock directly; never Step Functions' direct image integration | COVERED | 01-02 Task 2 |
| D-07 pass bar is a schema-valid response, accuracy deferred to Phase 2 | COVERED | 01-02 Task 3 |
| D-08 model = whichever grants inference access first in ap-south-1 | COVERED | 01-02 Task 1 |
| D-09 smoke test gated on a usable AWS account | COVERED | 01-02 plan-level `depends_on` and Task 3 `<precondition>` |

**HANDOFF §14 status-checklist items falling inside this phase's window**

| Item | Status | Where |
|---|---|---|
| Builder Center student verification (under the new email) | COVERED | 01-03 Task 2, human confirmation |
| Exact submission deadline from the event page | COVERED | 01-03 Task 2 |
| 10-minute GitHub prior-art search ("before Friday" — today) | COVERED | 01-03 Task 3 |
| Extraction eval / model choice | out of scope here | Phase 2 |
| Tamil label review | out of scope here | Phase 4 |

**Exclusions (not gaps):** repo-visibility and skeleton-scope were Deferred Ideas explicitly delegated to planner discretion — both are resolved in `docs/DECISIONS.md` entries (f) and (i) rather than left open. Q1-Q5 outreach mechanics were deferred to execution; the default of asking in person is applied in 01-03 Task 2.

**Result: no unplanned items.**
</source_coverage_audit>

<verification>
- The `owed-foundation` stack exists in ap-south-1 and its `ApiUrl` output returns HTTP 200 with `"region": "ap-south-1"` in the body.
- The working tree matches the HANDOFF §11 layout, and `git ls-files testdata` returns exactly `testdata/README.md`.
- `docs/DECISIONS.md` has at least nine dated entries citing D-02, D-03, D-05, D-06, D-07 and D-08.
- No commit in the repository predates 2026-09-17.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 1 (a deployed ap-south-1 endpoint returns HTTP 200) and criterion 5 (repo skeleton matching HANDOFF §11 with a post-kickoff first commit and `docs/DECISIONS.md` started) are both demonstrably true, and plans 01-02 and 01-03 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-kickoff/01-01-SUMMARY.md` when done.
</output>
