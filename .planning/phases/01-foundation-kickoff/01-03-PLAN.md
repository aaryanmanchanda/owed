---
phase: 01-foundation-kickoff
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/fixtures/credits_sample.csv
  - tests/fixtures/README.md
  - .planning/intel/context.md
autonomous: false
requirements: [SC-3, SC-4, D-04]

user_setup:
  - service: aws-builder-center
    why: "HANDOFF §14 pre-event checklist requires student verification, and D-03 moved everything to a new account under a different email — RESEARCH assumption A3 flags it as genuinely unknown whether the verification carries over."
    dashboard_config:
      - task: "Sign in and check whether student verification shows as complete under the new account's email, and redo it if not"
        location: "AWS Builder Center -> profile -> verification status"
  - service: wemakedevs-first-commit
    why: "The hackathon's $100 AWS credit form and the exact Sunday submission deadline are both outside this repo and both open items (RESEARCH assumption A4 and open question 3)."
    dashboard_config:
      - task: "Submit the $100 AWS credit form with the NEW account ID and check whether it is accepted under the new email"
        location: "https://www.wemakedevs.org/aws/first-commit — credit form linked from the event page"
      - task: "Read the exact Sunday cutoff time and timezone off the event page or submission portal"
        location: "https://www.wemakedevs.org/aws/first-commit and the submission portal itself"
  - service: github
    why: "Submission requires a public repo (HANDOFF §1); no git remote is configured yet, and the rough submission lands Saturday."
    dashboard_config:
      - task: "Create a public GitHub repository named owed and push main to it"
        location: "github.com/new, or `gh repo create owed --public --source=. --push`"

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Between 20 and 30 photos of Aaryan's own UPI payments exist under the git-ignored testdata/photos/, spanning straight-on, roughly 30-degree angle, overhead glare, dim light and partly-cut-off conditions across GPay, PhonePe and Paytm (ROADMAP SC-3, HANDOFF §8)."
    - "No fabricated, generated or customer-owned payment screenshot exists anywhere in the repo or the test set (HANDOFF §6, §15 rule 4 — a hard rule, no exceptions)."
    - "A shape-faithful, value-scrubbed payment-record fixture exists at tests/fixtures/credits_sample.csv including at least one row with no reference number (HANDOFF §10 parser test requirement)."
    - "Aaryan's raw bank transaction export is present locally for Phase 2 ground truth but is not tracked by git."
    - "Each of Q1 through Q5 carries either a recorded answer or an explicit still-open status with a date in .planning/intel/context.md (ROADMAP SC-4)."
    - "The HANDOFF §14 items that fall in this phase's window — Builder Center verification under the new email, $100-credit eligibility, the exact Sunday deadline, and the 10-minute GitHub prior-art search — each carry a recorded outcome or an explicit still-open status."
  artifacts:
    - tests/fixtures/credits_sample.csv
    - tests/fixtures/README.md
    - .planning/intel/context.md
  key_links:
    - "credits_sample.csv column shape -> the Phase 3 parser interface: the fixture is provisional until Q1 answers what format Brindhavan actually has, and the parser is written behind an interface precisely because of that (HANDOFF §2, §13 risk 3)."
    - "Q2's answer -> timestamp handling: regardless of the answer, on-screen screen_time is used and file metadata never is (HANDOFF §2)."
    - "Q4's answer -> the video's opening 20 seconds and the [REAL DETAIL FROM Q4] placeholder in the §3 pitch."
    - "Q5's answer -> whether the restaurant can be named or filmed at all; absent consent the writeup says 'a small restaurant near my building'."
    - "The photo set -> Phase 2's entire evaluation: too few photos or missing conditions makes the §8 measurement unrepresentative and the glare/angle pitch claim unsupportable."
---

<objective>
Produce the real, non-fabricated inputs everything downstream depends on, and get the five questions that shape the build in front of Hotel Brindhavan's owner.

Per D-04 this runs in parallel with the AWS work in plan 01-02 rather than behind it — Bedrock model access on a brand-new account may not be instant, and there is no reason for the photo set and the owner conversation to wait on it. Per D-01 it is day 2 of a 3.5-day event, so this plan is deliberately thin: take the photos, ask the questions, write down what came back, and move.

Two of the items here are explicitly outside this project's control and are NOT to be solved in code: whether Builder Center student verification carries across to the new account's email (RESEARCH assumption A3), and whether the hackathon's $100 credit is granted under that email (assumption A4). Task 2 confirms them as human steps and records the outcome; nothing programmatic attempts to resolve them.

Purpose: satisfies ROADMAP Phase 1 success criteria 3 and 4, and supplies Phase 2's evaluation set and Phase 3's parser fixture.
Output: 20-30 real test photos plus a transaction export held outside git, a scrubbed credits fixture, and an updated open-questions and status record in `.planning/intel/context.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/intel/context.md
@.planning/phases/01-foundation-kickoff/01-CONTEXT.md
@.planning/phases/01-foundation-kickoff/01-01-SUMMARY.md
@HANDOFF.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Capture the 20-30 photo test set and the transaction export</name>
  <files>testdata/photos/, testdata/export/ (both git-ignored; no repo file is written by this task)</files>
  <precondition>`.gitignore` covering `testdata/*` is committed (plan 01-01 Task 2) — without it, the first `git add` after this task would commit real bank photos permanently into a repo destined to be public.</precondition>
  <read_first>
    - `HANDOFF.md` §8 "Test set" — the exact condition matrix (straight on, about 30 degrees, overhead glare, dim light, partly cut off) and app coverage (GPay, PhonePe, Paytm, more if available) that Phase 2 measures against.
    - `HANDOFF.md` §6 "Data rules" — photos come from Aaryan's own small transfers photographed off a phone screen; no customer payment screens anywhere; no fake-screenshot generator, ever.
    - `testdata/README.md` — what belongs in the git-ignored tree and why.
  </read_first>
  <action>Obtain the 20-30 real test photos and the ground-truth transaction export. Photographing a payment screen is physical, and HANDOFF §6 and §15 rule 4 forbid every substitute — no generated images, no downloaded samples, no customer screens, no exceptions. An executor tempted to synthesise a stand-in would be violating the project's single hardest rule. Present the instructions below and wait; there is nothing to automate first.</action>
  <instructions>
    This one is entirely yours — there is no tool that can produce this data, and the one thing that could (a screenshot generator) is explicitly forbidden by HANDOFF §6 and §15 rule 4.

    Make small UPI transfers of your own money — to yourself, or small real payments you were going to make anyway — across GPay, PhonePe and Paytm. Photograph each payment-success screen off the phone display using a second phone. A photograph of a screen, not a screenshot: a camera photo of another phone's display is the actual input this product receives, and it is the only thing Phase 2's evaluation can honestly measure.

    Aim for 20 to 30 photos spread across all five conditions HANDOFF §8 names: straight on, at roughly a 30-degree angle, with overhead glare, in dim light, and partly cut off. Each condition should appear several times — a set of 25 clean straight-on shots can neither support nor refute the pitch's glare-and-angle claim, which makes it useless to Phase 2 either way.

    Save every photo under `testdata/photos/` in the repo root. That directory is git-ignored on purpose: these show your own bank details and must never enter a public repository.

    Separately, export the matching transaction history from the bank or payment app, in whatever format it comes in, and save it under `testdata/export/`. Phase 2 uses it as ground truth for the extraction evaluation (HANDOFF §8: correct values come from your own transaction history), and Task 3 derives a value-scrubbed fixture from its column shape. The raw export stays out of git; only the scrubbed derivative is ever committed.

    When you are done, tell me the final photo count and which of the five conditions and three apps you managed to cover.
  </instructions>
  <verification>Between 20 and 30 image files exist under `testdata/photos/`, a file exists under `testdata/export/`, and `git status --porcelain` reports nothing for either path.</verification>
  <resume-signal>Type "done" with the photo count and the conditions and apps covered, or tell me what you could not get.</resume-signal>
  <acceptance_criteria>
    - Between 20 and 30 image files exist under `testdata/photos/`.
    - A transaction export file exists under `testdata/export/`.
    - `git status --porcelain` shows no untracked or staged file under `testdata/photos/` or `testdata/export/`.
    - The user has confirmed, in the session, that all five HANDOFF §8 conditions and at least the three named apps are represented.
    - No file in the set was generated by any tool; all are camera photographs of Aaryan's own payment screens.
  </acceptance_criteria>
  <verify>
    <automated>N=$(ls testdata/photos/ 2>/dev/null | grep -Eci '\.(jpe?g|png|heic)$') && echo "PHOTO_COUNT=$N" && test "$N" -ge 20 && test "$N" -le 30 && test -n "$(ls testdata/export/ 2>/dev/null)" && test -z "$(git status --porcelain -- testdata/photos testdata/export)" && echo TESTSET_OK</automated>
    <fails_when>non-zero exit; `TESTSET_OK` absent; the printed `PHOTO_COUNT` being below 20 (the set is too small for Phase 2's evaluation to be representative) or above 30 (over-collection burns Phase 2's Bedrock budget for no measurement gain); `ls testdata/export/` printing nothing (no ground-truth export exists); or `git status --porcelain` printing any line for those paths, which means real bank photos are staged or untracked-but-visible to git and the `.gitignore` rule is not doing its job.</fails_when>
  </verify>
  <done>20-30 real camera photographs of Aaryan's own UPI payment screens spanning the HANDOFF §8 condition and app matrix exist locally, alongside his raw transaction export, and git can see none of them. ROADMAP Phase 1 success criterion 3's data half is met.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Ask Q1-Q5 and close out the hackathon logistics items</name>
  <files>none in the repo — outcomes are reported back in-session and written to `.planning/intel/context.md` by Task 3; the git remote is configured outside the working tree</files>
  <read_first>
    - `HANDOFF.md` §2 "Open questions for Brindhavan" — the five questions and, for each, why the answer changes the build.
    - `HANDOFF.md` §14 "Status checklist" — the items still blank in source, including Builder Center verification and the exact submission deadline.
    - `.planning/phases/01-foundation-kickoff/01-RESEARCH.md` "Common Pitfalls" 4 and assumptions A3 and A4 — why the account swap leaves Builder Center and the $100 credit genuinely unresolved rather than fine-by-default.
    - `.planning/intel/context.md` topic "Open questions for Brindhavan" — the record Task 3 updates.
  </read_first>
  <action>Get Q1-Q5 in front of Hotel Brindhavan's owner and close out the four HANDOFF §14 logistics items that fall in this phase's window. Both halves are human-only: one is a conversation with a restaurant owner, the other is signing in to third-party accounts and reading pages behind authentication. Present the instructions below and wait.</action>
  <instructions>
    Two halves, both yours — I can't have a conversation with the owner, and the rest sits behind logins I don't have.

    Half one, the owner conversation. Go to Hotel Brindhavan in person — the discretion note in `01-CONTEXT.md` defaults to in person since Aaryan lives in the delivery building, and an in-person ask gets Q4 and Q5 answered far better than a message would. Ask all five, and do not let a half-answer pass as an answer:
    Q1, how the owner actually sees incoming UPI money — a bank app statement and whether it exports to CSV or PDF, a PhonePe or Paytm business app, SMS, or a soundbox. This decides the parser and is HANDOFF §13's third-biggest build risk. If he can export a sample, ask for one.
    Q2, how rider photos reach him — WhatsApp or something else. Whatever the answer, the build uses the time shown on the payment screen and never file metadata, so this changes how confident the timestamps are, not which field is read.
    Q3, riders per shift, orders per shift, and how cash is settled today. This sets the demo's scale and the results screen's layout.
    Q4, one real example of a mismatch and how long checking takes now. This is the video's first 20 seconds and fills the `[REAL DETAIL FROM Q4]` placeholder in the §3 pitch. If the honest answer is that it takes five minutes and has never been a problem, that matters and must be reported back rather than smoothed over.
    Q5, consent to name the restaurant and film him briefly. Without consent the writeup and video say "a small restaurant near my building" instead.

    Half two, the logistics that the AWS account swap put in doubt. These are outside this project's control; confirm them, do not try to engineer around them.
    Sign in to AWS Builder Center under the NEW account's email and check whether student verification shows as complete or needs redoing (HANDOFF §14, RESEARCH assumption A3 — genuinely unknown, do not assume it carries over).
    Submit the hackathon's $100 AWS credit form with the new account ID and note whether it is accepted under the new email (RESEARCH assumption A4). If it is not, Phase 2's evaluation cost comes out of free tier and personal spend — a small amount, but it should be a known fact rather than a surprise.
    Read the exact Sunday cutoff time and timezone off the event page at wemakedevs.org/aws/first-commit and, if the page does not state it, off the submission portal itself. This has been an open item since ingest and Phase 5's final submit depends on it.
    Create a public GitHub repository and push `main` to it, per the repo-visibility decision recorded in `docs/DECISIONS.md`. Pushing today rather than Saturday de-risks the rough submission and makes the post-kickoff commit history publicly verifiable.

    Report back each of the nine items with either the answer or an explicit "still open". A blank is not an acceptable outcome; an honest "still open" is.
  </instructions>
  <verification>`git remote -v` lists a github.com origin and `git log @{u}..HEAD` is empty, and the user has reported an answer or explicit still-open status for each of Q1-Q5, Builder Center verification, $100-credit eligibility, and the exact Sunday deadline.</verification>
  <resume-signal>Reply with the nine outcomes (answer or "still open" for each), then type "done".</resume-signal>
  <acceptance_criteria>
    - The user has reported, in the session, a specific answer or an explicit still-open status for each of Q1, Q2, Q3, Q4 and Q5.
    - The user has reported the Builder Center verification status under the new account's email.
    - The user has reported whether the $100 credit form accepted the new account.
    - The user has reported the exact Sunday deadline time and timezone, or that the event page and portal both omit it.
    - `git remote -v` lists an origin pointing at a GitHub repository, and `git status` reports the local branch is not ahead of it.
  </acceptance_criteria>
  <verify>
    <automated>git remote -v > /tmp/owed-remotes.txt; test $? -eq 0 || { echo GIT_REMOTE_FAILED; exit 1; }; grep -q 'github.com' /tmp/owed-remotes.txt && git rev-parse --abbrev-ref --symbolic-full-name '@{u}' && git log '@{u}'..HEAD --oneline > /tmp/owed-unpushed.txt && test ! -s /tmp/owed-unpushed.txt && echo REPO_PUBLISHED</automated>
    <fails_when>non-zero exit; `REPO_PUBLISHED` absent; `GIT_REMOTE_FAILED` printed; no `github.com` line in the remotes (the public repo was never created); `no upstream configured` from `git rev-parse` (the branch was never pushed); or `/tmp/owed-unpushed.txt` being non-empty, meaning local commits have not reached the remote and the public repo does not yet show the real post-kickoff history HANDOFF §1 requires.</fails_when>
  </verify>
  <done>All five Brindhavan questions have been put to the owner with an answer or an explicit still-open status for each, the four HANDOFF §14 logistics items falling in this phase's window have a recorded outcome, and the repository is public with its post-kickoff history pushed.</done>
</task>

<task type="auto">
  <name>Task 3: Record the answers, build the scrubbed credits fixture, and run the prior-art search</name>
  <files>tests/fixtures/credits_sample.csv, tests/fixtures/README.md, .planning/intel/context.md</files>
  <read_first>
    - `.planning/intel/context.md` topics "Open questions for Brindhavan", "Status checklist" and "Prior art" — the three sections this task updates in place.
    - `HANDOFF.md` §10 "Tests" — the parser test requirement that the fixture must include a row with no reference.
    - `HANDOFF.md` §7 — the twelve-digit UTR shape (`^\d{12}$`) the fixture's reference column must exercise.
    - `HANDOFF.md` §5 "Prior art already found" — the existing table and the four GitHub search terms the source says to run before Friday.
    - `tests/fixtures/README.md` — the synthetic-only rule governing the directory this task writes into.
    - `testdata/export/` — the raw export whose COLUMN SHAPE (not values) the fixture reproduces.
  </read_first>
  <action>
    Turn everything the two checkpoints surfaced into written record, and build the one committable artifact.

    First, the fixture. Read only the header row and the general column layout of the raw export under `testdata/export/` — do not copy any real value out of it. Write `tests/fixtures/credits_sample.csv` with the same column names and column order as the real export, filled entirely with invented values. If the export is unavailable or in a binary format, default to the common Indian bank statement shape: a transaction date column, a value date column, a narration column, a reference-number column, a debit column, a credit column and a running balance column. The file needs at least twelve data rows and must include, at minimum: several UPI credit rows whose reference column holds a distinct invented twelve-digit number matching `^\d{12}$`; at least one credit row whose reference column is EMPTY, because HANDOFF §10 requires the parser to be tested against a row with no reference and HANDOFF §9 specifies a hashed `CREDIT#H#` key for exactly that case; at least one debit row, so the parser has to discriminate; and amounts written with two decimal places. Invented names in the narration column must be obviously fictional. No real account number, no real balance, no real counterparty (threat T-01-12).

    Append a provenance note to `tests/fixtures/README.md` stating that `credits_sample.csv` reproduces the column shape of Aaryan's own export with every value replaced, that it is PROVISIONAL until Q1 confirms what format Hotel Brindhavan actually uses, and that Phase 3's parser is written behind an interface for precisely that reason (HANDOFF §2, §13 risk 3).

    Second, the prior-art search HANDOFF §5 says to run before Friday — today. Run the four searches the source names: rider cash settlement, UTR screenshot reconcile, payment screenshot bank statement, and COD reconciliation UPI. Use `gh search repos` if the GitHub CLI is authenticated, otherwise search github.com directly. Spend ten minutes, not an hour. Append the outcome to the "Prior art" topic in `.planning/intel/context.md`: either the specific repositories found that are closer to this project than anything already in the HANDOFF §5 table, each with how it differs, or an explicit dated line recording that the four searches were run and surfaced nothing closer. A negative result recorded honestly is a complete result; leaving the item unchecked is not.

    Third, the record itself. Update `.planning/intel/context.md` in place, keeping its existing topic structure:
    In the "Open questions for Brindhavan" topic, each of the five questions currently ends with an unanswered-status marker (visible in the file listed in `read_first`). Replace every one of those markers with the answer the owner gave plus the date, or with an explicit still-open line plus the date and, where the owner gave a partial answer, what remains unknown. Where an answer changes a downstream constraint — Q1 naming a concrete record format, or Q5 withholding consent — say so in the same line so Phase 3 and Phase 5 inherit it without re-deriving it.
    In the "Status checklist" topic, fill the Builder Center verification, exact submission deadline and prior-art-search entries with what Task 2 and this task established, and leave the extraction-eval and Tamil-review entries untouched — those belong to Phase 2 and Phase 4.
    Add a short dated note recording the $100-credit outcome, since an ineligible result changes how Phase 2's Bedrock spend is budgeted.

    Do not edit `docs/DECISIONS.md` in this plan. Plan 01-02 owns that file in this same wave, and a concurrent edit would collide. These are recorded facts and status updates rather than decisions; the one genuine decision they imply — which payment-record format the parser targets — is logged by Phase 3 once Q1's answer is actually in hand.

    Commit as one atomic commit.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/credits_sample.csv` exists with a header row and at least twelve data rows.
    - At least one data row has an empty reference column.
    - At least two distinct reference values match `^\d{12}$`.
    - At least one row is a debit rather than a credit.
    - No string of 9 or more consecutive digits appears outside the reference column (a crude guard against a real account number surviving the scrub).
    - `.planning/intel/context.md` contains no remaining `STATUS: unanswered` marker for Q1 through Q5.
    - `.planning/intel/context.md` names each of Q1, Q2, Q3, Q4 and Q5 with a 2026-09-18 date against it.
    - The "Prior art" topic in `.planning/intel/context.md` carries a dated line recording the four-search outcome.
    - `tests/fixtures/README.md` states that the fixture is provisional pending Q1.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import csv,re,sys
rows=list(csv.reader(open('tests/fixtures/credits_sample.csv')))
hdr,data=rows[0],rows[1:]
bad=[]
if len(data)<12: bad.append('TOO_FEW_ROWS')
flat=[c.strip() for r in data for c in r]
utrs=[c for c in flat if re.fullmatch(r'\d{12}',c)]
if len(set(utrs))<2: bad.append('TOO_FEW_UTRS')
if not any(any(c.strip()=='' for c in r) for r in data): bad.append('NO_BLANK_REFERENCE_ROW')
long=[c for c in flat if re.search(r'\d{9,}',c) and not re.fullmatch(r'\d{12}',c)]
if long: bad.append('POSSIBLE_REAL_ACCOUNT_NUMBER '+str(long[:3]))
print('HEADER',hdr)
sys.exit(' | '.join(bad)) if bad else print('FIXTURE_OK',len(data),'rows',len(set(utrs)),'utrs')
"</automated>
    <fails_when>non-zero exit; `FIXTURE_OK` absent; `TOO_FEW_ROWS` printed; `TOO_FEW_UTRS` printed (fewer than two distinct twelve-digit references, so the duplicate-UTR and matching paths cannot be exercised); `NO_BLANK_REFERENCE_ROW` printed, which is the HANDOFF §10 case the Phase 3 parser is explicitly required to handle; or `POSSIBLE_REAL_ACCOUNT_NUMBER` printed with the offending values, meaning a long digit string that is not a twelve-digit UTR survived the scrub.</fails_when>
    <automated>python3 -c "
import re,sys
t=open('.planning/intel/context.md').read()
bad=[]
if 'STATUS: unanswered' in t: bad.append('UNANSWERED_MARKER_REMAINS')
for q in ['Q1','Q2','Q3','Q4','Q5']:
    seg=[l for l in t.splitlines() if l.strip().startswith('- '+q+':') or l.strip().startswith(q+':') or ('**'+q+'**') in l]
    if not seg: bad.append('NO_LINE_FOR_'+q); continue
    if not any('2026-09-18' in l for l in seg): bad.append('NO_DATE_ON_'+q)
if not re.search(r'2026-09-18.*(prior[- ]art|GitHub search)', t, re.I|re.S): bad.append('NO_PRIOR_ART_RESULT')
sys.exit(' | '.join(bad)) if bad else print('RECORD_OK')
"</automated>
    <fails_when>non-zero exit; `RECORD_OK` absent; `UNANSWERED_MARKER_REMAINS` printed (at least one question still carries its original unanswered marker, so ROADMAP SC-4 is not met); `NO_LINE_FOR_Qn` printed naming a question that vanished from the record entirely; `NO_DATE_ON_Qn` printed, meaning the status was changed without a date and cannot be told apart from the pre-outreach state; or `NO_PRIOR_ART_RESULT` printed because the four HANDOFF §5 searches left no dated outcome.</fails_when>
    <automated>grep -qi 'provisional' tests/fixtures/README.md && grep -q 'Q1' tests/fixtures/README.md && echo FIXTURE_CAVEAT_OK</automated>
    <fails_when>non-zero exit or `FIXTURE_CAVEAT_OK` absent — the fixtures README does not warn that `credits_sample.csv` is provisional pending Q1, so Phase 3 could mistake an invented column layout for Brindhavan's confirmed format.</fails_when>
  </verify>
  <done>A committable, value-scrubbed credits fixture including a no-reference row exists at `tests/fixtures/credits_sample.csv`, every Q1-Q5 status in `.planning/intel/context.md` carries a dated answer or explicit still-open note, the HANDOFF §14 items in this phase's window are recorded, and the four-search prior-art result is written down. ROADMAP Phase 1 success criteria 3 and 4 are met.</done>
  <reversibility rating="reversible">The fixture is explicitly provisional and is expected to be replaced once Q1 lands; the Phase 3 parser sits behind an interface precisely so that replacement costs nothing structural.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01-03)

| Artifact | Kind | Notes |
|---|---|---|
| `testdata/photos/*` | Test data, git-ignored | 20-30 camera photographs of Aaryan's own UPI payment-success screens across five conditions and three apps. Phase 2's entire evaluation set. Never committed. |
| `testdata/export/*` | Ground-truth data, git-ignored | Aaryan's raw transaction export. Phase 2's correctness reference. Never committed. |
| `tests/fixtures/credits_sample.csv` | Committed fixture | Shape-faithful, value-scrubbed credits fixture; 12+ rows, 2+ distinct 12-digit UTRs, at least one blank-reference row (HANDOFF §10), at least one debit row. Phase 3's parser fixture. |
| `tests/fixtures/README.md` (extended) | Doc | Provenance note plus the provisional-pending-Q1 caveat. |
| `.planning/intel/context.md` (updated) | Planning record | Q1-Q5 dated answers or still-open statuses; HANDOFF §14 status-checklist entries for Builder Center, deadline and prior-art search; $100-credit outcome note; prior-art search result. |
| Public GitHub remote for this repo | External | Created and pushed in Task 2; makes the post-kickoff commit history publicly verifiable (HANDOFF §1). |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aaryan's banking data -> working tree -> public git history | Photos and the raw export contain his real account activity; the repo is public. |
| Restaurant owner / third parties -> project record | Q1-Q5 outreach touches another person's business information and, via Q5, their consent to be named and filmed. |
| Raw bank export -> committed test fixture | The fixture is derived from a real export; a careless derivation leaks real values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information Disclosure | `tests/fixtures/credits_sample.csv` derived from a real bank export | high | mitigate | Only the header row and column order are read from the real export; every value is invented. Task 3's fixture check fails the task on any digit string of 9 or more characters that is not a 12-digit UTR, catching an account number or a real reference that survived the scrub. `tests/fixtures/README.md` states the synthetic-only rule for the directory. |
| T-01-13 | Information Disclosure | Real photos and the raw export under `testdata/` | high | mitigate | `.gitignore` from plan 01-01 covers `testdata/*` with a single `!testdata/README.md` negation; Task 1 additionally fails on any `git status --porcelain` output for `testdata/photos` or `testdata/export`, so a rule that stopped working is caught before the commit rather than after the push. |
| T-01-14 | Information Disclosure | Third-party PII gathered during Q1-Q5 outreach | medium | mitigate | Only business-process facts are recorded, never customer payment details or customer screens (HANDOFF §6). Q5 consent is obtained and recorded BEFORE the restaurant is named or filmed anywhere; absent consent, PROJECT.md's fallback phrasing is used in the writeup and video. |
| T-01-15 | Spoofing | Fabricated or downloaded payment screenshots substituted for real photos | high | mitigate | HANDOFF §6 and §15 rule 4 forbid any fake-screenshot generator outright. Task 1 is a blocking human checkpoint whose action text names the substitution temptation explicitly so an executor cannot rationalise it, and the photo count is asserted against the physical set rather than against anything a tool could produce. |
| T-01-16 | Repudiation | Unverifiable claim that Q1-Q5 were actually asked | low | accept | Each answer or still-open status is recorded with a 2026-09-18 date in `.planning/intel/context.md` and the verify command fails on an undated entry. No stronger evidence trail is warranted at ASVS L1 for a solo hackathon record. |
| T-01-17 | Information Disclosure | Publishing the repo publicly exposes `.planning/` working notes | low | accept | `.planning/` contains this project's own design reasoning and no credentials or personal banking data; publishing it is deliberate and shows honest post-kickoff process to judges. The `.gitignore` rules keep the actual sensitive material (`testdata/`, `.env*`, `*.pem`) out. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. `01-RESEARCH.md` "Package Legitimacy Audit" covers the five project packages; all `[SUS]` verdicts are documented heuristic false positives overridden to OK against official org-owned repositories, no `[SLOP]` package exists, and no unresolved `[ASSUMED]`/`[SUS]` package remains, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- `testdata/photos/` holds 20-30 images and `testdata/export/` holds the transaction export, and `git status --porcelain` reports nothing for either path.
- `tests/fixtures/credits_sample.csv` has 12 or more data rows, two or more distinct 12-digit references, at least one blank-reference row, and no stray long digit string.
- `.planning/intel/context.md` carries a dated answer or explicit still-open status for each of Q1-Q5, with no original unanswered marker left.
- The prior-art search outcome is recorded with a date, even if the result was that nothing closer exists.
- `git remote -v` shows a GitHub origin and the local branch is not ahead of it.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 3 (20-30 real test photos plus a fixture built from Aaryan's own transaction export, with no fabricated or customer data anywhere) and criterion 4 (Q1-Q5 asked, with whatever is answered or still open recorded rather than silently assumed) are both demonstrably true, and Phase 2 has its evaluation set while Phase 3 has its parser fixture.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-kickoff/01-03-SUMMARY.md` when done.
</output>